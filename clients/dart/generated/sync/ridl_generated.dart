// Generated by ridl from sync.route-map.json. DO NOT EDIT.
// Edit the route map and re-run `ridl generate`; a drift check fails the build if this file and the map disagree.
//
// service: hhm-sync
// version: 0.1.0
//
// Scalar newtypes are extension types: free at runtime, distinct to the analyzer.

// ignore_for_file: unnecessary_cast, unused_element

import 'dart:convert';

const String kService = "hhm-sync";
const String kVersion = "0.1.0";

// ---------------------------------------------------------------- types

/// Built inline in the route closure with serde_json::json!.
class Health {
  const Health({required this.status, required this.service});

  factory Health.fromJson(Map<String, Object?> json) {
    return Health(
      status: json["status"] as String,
      service: json["service"] as String,
    );
  }

  final String status;
  final String service;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "status": status,
      "service": service,
    };
  }
}

class ReconcileRequest {
  const ReconcileRequest({required this.base, required this.incoming});

  factory ReconcileRequest.fromJson(Map<String, Object?> json) {
    return ReconcileRequest(
      base: json["base"],
      incoming: json["incoming"],
    );
  }

  /// The document already held. Any JSON value; in practice an array of reservation records.
  final Object? base;
  /// The document to merge in. Any JSON value, of the same shape as `base`.
  final Object? incoming;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "base": base,
      "incoming": incoming,
    };
  }
}

class ReconcileResponse {
  const ReconcileResponse({required this.merged, required this.engine, required this.contract});

  factory ReconcileResponse.fromJson(Map<String, Object?> json) {
    return ReconcileResponse(
      merged: json["merged"],
      engine: json["engine"] as String,
      contract: json["contract"] as String,
    );
  }

  /// The merged document.
  final Object? merged;
  /// Constant `opto-sync/syncer.rs`.
  final String engine;
  /// Constant `hhm_interfaces::Reservation` -- what the server claims it merges. Note that hhm-interfaces::Reservation (title/summary/space_name/starts_at/ends_at) is not the shape hhm-api actually serves (room_type/check_in/check_out/workspace_plan/notes); this string is a label, not a checked contract.
  final String contract;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "merged": merged,
      "engine": engine,
      "contract": contract,
    };
  }
}

/// Returned with 422 when the two documents cannot be merged.
class MergeError {
  const MergeError({required this.error, required this.message});

  factory MergeError.fromJson(Map<String, Object?> json) {
    return MergeError(
      error: json["error"] as String,
      message: json["message"] as String,
    );
  }

  final String error;
  final String message;

  Map<String, Object?> toJson() {
    return <String, Object?>{
      "error": error,
      "message": message,
    };
  }
}

// ------------------------------------------------------------- transport

/// How a call reaches the server.
enum Delivery {
  direct('direct'),
  optoSyncQueued('opto_sync_queued');

  const Delivery(this.wire);

  final String wire;
}

/// Where a queued call's opto-sync record id comes from.
enum RecordIdFrom {
  path, request, minted;
}

/// Static description of a queued call's opto-sync binding.
class OptoSyncBinding {
  const OptoSyncBinding({required this.table, required this.operation, required this.from, this.name});

  final String table;
  final String operation;
  final RecordIdFrom from;
  final String? name;
}

/// One outbound call, fully resolved. Transports never re-derive paths.
class RpcRequest {
  const RpcRequest({
    required this.key,
    required this.method,
    required this.path,
    required this.pathTemplate,
    this.query = const [],
    this.body,
    this.delivery = Delivery.direct,
    this.optoSync,
  });

  final String key;
  final String method;

  /// Path with every parameter already substituted and encoded.
  final String path;

  /// The unsubstituted template, so a transport can locate a named
  /// parameter inside [path] instead of guessing at its position.
  final String pathTemplate;
  final List<MapEntry<String, String>> query;

  /// JSON body, or null for operations that carry none.
  final String? body;
  final Delivery delivery;
  final OptoSyncBinding? optoSync;
}

/// The single seam between generated code and the network.
///
/// Implement it over HTTP for direct calls and over an opto-sync queue for
/// queued ones. The dependency runs one way: this library calls opto-sync,
/// never the reverse.
abstract interface class RpcTransport {
  Future<String> call(RpcRequest request);
}

/// Encode one path segment so an id containing `/` cannot reshape the URL.
String encodeSegment(String value) => Uri.encodeComponent(value);

String _queryValue(Object? value) => value is String ? value : '$value';

// ------------------------------------------------------------- operations

/// `GET /healthz`
String healthzPath() => "/healthz";

/// `GET /readyz`
String readyzPath() => "/readyz";

/// `POST /v1/reconcile`
String reconcilePath() => "/v1/reconcile";

/// `POST /api/v1/reconcile`
String reconcileApiAliasPath() => "/api/v1/reconcile";

/// Liveness.
Future<Health> healthz(RpcTransport transport) async {
  final path = healthzPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "healthz",
    method: "GET",
    path: path,
    pathTemplate: "/healthz",
    query: query,
    delivery: Delivery.direct,
  ));
  return Health.fromJson(jsonDecode(raw) as Map<String, Object?>);
}

/// Readiness probe. Answers 204 with an empty body, so there is no response type.
Future<void> readyz(RpcTransport transport) async {
  final path = readyzPath();
  const query = <MapEntry<String, String>>[];
  final raw = await transport.call(RpcRequest(
    key: "readyz",
    method: "GET",
    path: path,
    pathTemplate: "/readyz",
    query: query,
    delivery: Delivery.direct,
  ));
  return;
}

/// Merge two documents and return the result.
Future<ReconcileResponse> reconcile(RpcTransport transport, ReconcileRequest bodyValue) async {
  final path = reconcilePath();
  const query = <MapEntry<String, String>>[];
  final body = jsonEncode(bodyValue.toJson());
  final raw = await transport.call(RpcRequest(
    key: "reconcile",
    method: "POST",
    path: path,
    pathTemplate: "/v1/reconcile",
    query: query,
    body: body,
    delivery: Delivery.direct,
  ));
  return ReconcileResponse.fromJson(jsonDecode(raw) as Map<String, Object?>);
}

/// The same handler, mounted a second time under /api. Kept because deployed callers use both prefixes.
Future<ReconcileResponse> reconcileApiAlias(RpcTransport transport, ReconcileRequest bodyValue) async {
  final path = reconcileApiAliasPath();
  const query = <MapEntry<String, String>>[];
  final body = jsonEncode(bodyValue.toJson());
  final raw = await transport.call(RpcRequest(
    key: "reconcile_api_alias",
    method: "POST",
    path: path,
    pathTemplate: "/api/v1/reconcile",
    query: query,
    body: body,
    delivery: Delivery.direct,
  ));
  return ReconcileResponse.fromJson(jsonDecode(raw) as Map<String, Object?>);
}

// -------------------------------------------------------------- manifest

/// One row per declared operation.
class OperationInfo {
  const OperationInfo({required this.key, required this.path, required this.methods, required this.delivery});

  final String key;
  final String path;
  final List<String> methods;
  final Delivery delivery;
}

/// Every operation in the route map, in declaration order.
const List<OperationInfo> operations = [
  OperationInfo(key: "healthz", path: "/healthz", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "readyz", path: "/readyz", methods: ["GET"], delivery: Delivery.direct),
  OperationInfo(key: "reconcile", path: "/v1/reconcile", methods: ["POST"], delivery: Delivery.direct),
  OperationInfo(key: "reconcile_api_alias", path: "/api/v1/reconcile", methods: ["POST"], delivery: Delivery.direct),
];

/// Operations that route through opto-sync's durable queue.
const List<String> queuedOperations = [];
