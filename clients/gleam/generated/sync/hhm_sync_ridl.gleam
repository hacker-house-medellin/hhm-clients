//// Generated by ridl from sync.route-map.json. DO NOT EDIT.
//// Edit the route map and re-run `ridl generate`; a drift check fails the build if this file and the map disagree.
////
//// service: hhm-sync
//// version: 0.1.0

import gleam/dict
import gleam/dynamic
import gleam/dynamic/decode
import gleam/json
import gleam/list
import gleam/option
import gleam/string

pub const service = "hhm-sync"
pub const version = "0.1.0"

/// Built inline in the route closure with serde_json::json!.
pub type Health {
  Health (
    status: String,
    service: String,
  )
}

pub fn health_decoder() -> decode.Decoder(Health) {
  use status <- decode.field("status", decode.string)
  use service <- decode.field("service", decode.string)
  decode.success(Health(status, service))
}

pub fn health_to_json(value: Health) -> json.Json {
  json.object([
    #("status", json.string(value.status)),
    #("service", json.string(value.service)),
  ])
}

pub type ReconcileRequest {
  ReconcileRequest (
    base: dynamic.Dynamic,
    incoming: dynamic.Dynamic,
  )
}

pub fn reconcile_request_decoder() -> decode.Decoder(ReconcileRequest) {
  use base <- decode.field("base", decode.dynamic)
  use incoming <- decode.field("incoming", decode.dynamic)
  decode.success(ReconcileRequest(base, incoming))
}

pub fn reconcile_request_to_json(value: ReconcileRequest) -> json.Json {
  json.object([
    #("base", json.null()(value.base)),
    #("incoming", json.null()(value.incoming)),
  ])
}

pub type ReconcileResponse {
  ReconcileResponse (
    merged: dynamic.Dynamic,
    engine: String,
    contract: String,
  )
}

pub fn reconcile_response_decoder() -> decode.Decoder(ReconcileResponse) {
  use merged <- decode.field("merged", decode.dynamic)
  use engine <- decode.field("engine", decode.string)
  use contract <- decode.field("contract", decode.string)
  decode.success(ReconcileResponse(merged, engine, contract))
}

pub fn reconcile_response_to_json(value: ReconcileResponse) -> json.Json {
  json.object([
    #("merged", json.null()(value.merged)),
    #("engine", json.string(value.engine)),
    #("contract", json.string(value.contract)),
  ])
}

/// Returned with 422 when the two documents cannot be merged.
pub type MergeError {
  MergeError (
    error: String,
    message: String,
  )
}

pub fn merge_error_decoder() -> decode.Decoder(MergeError) {
  use error <- decode.field("error", decode.string)
  use message <- decode.field("message", decode.string)
  decode.success(MergeError(error, message))
}

pub fn merge_error_to_json(value: MergeError) -> json.Json {
  json.object([
    #("error", json.string(value.error)),
    #("message", json.string(value.message)),
  ])
}

//// ---------------------------------------------------- transport

/// How a call reaches the server.
pub type Delivery {
  Direct
  OptoSyncQueued
}

/// Where a queued call's opto-sync record id comes from.
pub type RecordIdSource {
  FromPath(name: String)
  FromRequest(name: String)
  Minted
}

/// Static description of a queued call's opto-sync record.
pub type OptoSyncBinding {
  OptoSyncBinding (
    table: String,
    operation: String,
    record_id: RecordIdSource,
  )
}

/// One outbound call, fully resolved. Transports never re-derive paths.
pub type RpcRequest {
  RpcRequest (
    key: String,
    method: String,
    path: String,
    path_template: String,
    query: List(#(String, String)),
    body: option.Option(String),
    delivery: Delivery,
    opto_sync: option.Option(OptoSyncBinding),
  )
}

/// The single seam between generated code and the network.
///
/// Supply a function that performs `request` and returns the raw JSON body.
/// The dependency runs one way: this module calls opto-sync, never the
/// reverse.
pub type Transport(e) =
  fn(RpcRequest) -> Result(String, e)

/// A call can fail in the transport or in decoding, and the two are
/// different problems for the caller.
pub type RpcError(e) {
  TransportError(e)
  DecodeError(List(decode.DecodeError))
}

/// Escape one path segment so an id containing `/` cannot reshape the URL.
pub fn encode_segment(value: String) -> String {
  value
  |> string.replace("%", "%25")
  |> string.replace("/", "%2F")
  |> string.replace("?", "%3F")
  |> string.replace("#", "%23")
  |> string.replace(" ", "%20")
}

/// `GET /healthz`
pub fn healthz_path() -> String {
  "/healthz"
}

/// `GET /readyz`
pub fn readyz_path() -> String {
  "/readyz"
}

/// `POST /v1/reconcile`
pub fn reconcile_path() -> String {
  "/v1/reconcile"
}

/// `POST /api/v1/reconcile`
pub fn reconcile_api_alias_path() -> String {
  "/api/v1/reconcile"
}

/// Liveness.
pub fn healthz_request() -> RpcRequest {
  let path = healthz_path()
  let query = []
  let body = option.None
  RpcRequest(
    key: "healthz",
    method: "GET",
    path: path,
    path_template: "/healthz",
    query: query,
    body: body,
    delivery: Direct,
    opto_sync: option.None,
  )
}

/// Decoder for the `healthz` success payload.
pub fn healthz_response_decoder() -> decode.Decoder(Health) {
  health_decoder()
}

/// Readiness probe. Answers 204 with an empty body, so there is no response type.
pub fn readyz_request() -> RpcRequest {
  let path = readyz_path()
  let query = []
  let body = option.None
  RpcRequest(
    key: "readyz",
    method: "GET",
    path: path,
    path_template: "/readyz",
    query: query,
    body: body,
    delivery: Direct,
    opto_sync: option.None,
  )
}

/// Merge two documents and return the result.
pub fn reconcile_request(body body: ReconcileRequest) -> RpcRequest {
  let path = reconcile_path()
  let query = []
  let body = option.Some(json.to_string(reconcile_request_to_json(body)))
  RpcRequest(
    key: "reconcile",
    method: "POST",
    path: path,
    path_template: "/v1/reconcile",
    query: query,
    body: body,
    delivery: Direct,
    opto_sync: option.None,
  )
}

/// Decoder for the `reconcile` success payload.
pub fn reconcile_response_decoder() -> decode.Decoder(ReconcileResponse) {
  reconcile_response_decoder()
}

/// The same handler, mounted a second time under /api. Kept because deployed callers use both prefixes.
pub fn reconcile_api_alias_request(body body: ReconcileRequest) -> RpcRequest {
  let path = reconcile_api_alias_path()
  let query = []
  let body = option.Some(json.to_string(reconcile_request_to_json(body)))
  RpcRequest(
    key: "reconcile_api_alias",
    method: "POST",
    path: path,
    path_template: "/api/v1/reconcile",
    query: query,
    body: body,
    delivery: Direct,
    opto_sync: option.None,
  )
}

/// Decoder for the `reconcile_api_alias` success payload.
pub fn reconcile_api_alias_response_decoder() -> decode.Decoder(ReconcileResponse) {
  reconcile_response_decoder()
}

/// One row per declared operation.
pub type OperationInfo {
  OperationInfo (
    key: String,
    path: String,
    methods: List(String),
    delivery: Delivery,
  )
}

/// Every operation in the route map, in declaration order.
pub fn operations() -> List(OperationInfo) {
  [
    OperationInfo("healthz", "/healthz", ["GET"], Direct),
    OperationInfo("readyz", "/readyz", ["GET"], Direct),
    OperationInfo("reconcile", "/v1/reconcile", ["POST"], Direct),
    OperationInfo("reconcile_api_alias", "/api/v1/reconcile", ["POST"], Direct),
  ]
}

/// Operations that route through opto-sync's durable queue.
pub fn queued_operations() -> List(String) {
  []
}
