[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "hhm-client"
version = "0.1.0"
description = "Hacker House Medellín Python 3 client"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.hatch.build.targets.wheel]
packages = ["src/hhm_client"]
