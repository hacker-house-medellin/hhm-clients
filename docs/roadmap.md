# Client roadmap

1. Generate typed OpenAPI clients from the infra Worker contract.
2. Add Rust, Dart, Go, Gleam, Erlang, TypeScript, and WASM packages.
3. Publish SDK conformance tests shared with `hhm-infra`.
4. Add examples for browser, mobile, CLI, and edge runtimes.
